{
  "identifier": "wbm001-governance-framework-generator",
  "name": "AI Governance Framework Generator",
  "version": "1.0.0",
  "description": "Generates tailored AI governance frameworks for Australian businesses, aligned with the six AI6 practices in Australia's Voluntary AI Safety Standard (NAIC Guidance for AI Adoption). Profiles an organisation by industry, size and AI usage, then returns a structured, informational framework preview covering Accountability and Impact Assessment, with a link to the full six-practice framework. A structured starting point for Australian organisations shaping their AI policy and governance approach. Presents frameworks, not advice.",
  "author": "Kentron",
  "authorUrl": "https://github.com/wbm001",
  "homepage": "https://aiframework.com.au",
  "category": "business-services",
  "tags": [
    "ai-governance",
    "compliance",
    "australia",
    "business-tools",
    "risk-management",
    "ai-policy"
  ],
  "tools": [
    {
      "name": "start_session",
      "description": "Begin generating an AI governance framework for an Australian business.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "submit_answers",
      "description": "Submit the completed organisation profile for an AI governance framework session.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "session_id": { "type": "string" },
          "contact": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "email": { "type": "string" },
              "organisation": { "type": "string" }
            }
          },
          "answers": {
            "type": "object",
            "properties": {
              "industry": { "type": "string" },
              "orgSize": { "type": "string" },
              "aiUseCases": { "type": "string" },
              "dataTypes": { "type": "string" }
            },
            "required": ["industry", "orgSize", "aiUseCases", "dataTypes"]
          }
        },
        "required": ["session_id", "answers"]
      }
    },
    {
      "name": "get_framework",
      "description": "Returns the free preview of a tailored AI governance framework, covering the first two of six AI6 practices (Accountability and Impact Assessment). Full six-practice framework available at aiframework.com.au.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "session_id": { "type": "string" }
        },
        "required": ["session_id"]
      }
    }
  ]
}
